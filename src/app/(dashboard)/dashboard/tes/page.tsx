import React from 'react';
import PageContainer from '@/components/layout/page-container';

const Page = async () => {
  // };
  return (
    <PageContainer scrollable={false}>
      <div className='flex flex-1 flex-col space-y-4'>test</div>
    </PageContainer>
  );
};

export default Page;
